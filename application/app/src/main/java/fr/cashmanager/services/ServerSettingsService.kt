package fr.cashmanager.services

interface ServerSettingsService {
    /**
     * @GET declares an HTTP GET request
     * @Path("setting") annotation on the productId parameter marks it as a
     * replacement for the {product} placeholder in the @GET path
     */
    /*
    @GET("/settings/{setting}")
    fun getSettings(@Path("setting") productId: String): Call<Setting>
     */
}